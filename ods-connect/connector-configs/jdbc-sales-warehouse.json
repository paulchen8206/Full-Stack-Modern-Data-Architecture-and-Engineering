{
  "name": "jdbc-sales-warehouse",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "topics": "sales_order,sales_order_line_item,customer_sales",
    "value.converter": "io.confluent.connect.avro.AvroConverter",
    "value.converter.schema.registry.url": "http://schema-registry:8081",
    "connection.url": "jdbc:postgresql://postgres:5432/analytics",
    "connection.user": "analytics",
    "connection.password": "analytics",
    "table.name.format": "landing.${topic}",
    "insert.mode": "insert",
    "auto.create": "true",
    "auto.evolve": "true",
    "batch.size": "3000",
    "delete.enabled": "false",
    "pk.mode": "none",
    "errors.tolerance": "all",
    "errors.log.enable": "true",
    "errors.log.include.messages": "true",
    "transforms": "castNumbers,convertOrderTimestamp,convertUpdatedAt",
    "transforms.castNumbers.type": "org.apache.kafka.connect.transforms.Cast$Value",
    "transforms.castNumbers.spec": "orderTotal:float64,unitPrice:float64,lineTotal:float64,totalSpent:float64",
    "transforms.convertOrderTimestamp.type": "org.apache.kafka.connect.transforms.TimestampConverter$Value",
    "transforms.convertOrderTimestamp.field": "orderTimestamp",
    "transforms.convertOrderTimestamp.target.type": "Timestamp",
    "transforms.convertOrderTimestamp.format": "yyyy-MM-dd'T'HH:mm:ss.SSSSSSXXX",
    "transforms.convertUpdatedAt.type": "org.apache.kafka.connect.transforms.TimestampConverter$Value",
    "transforms.convertUpdatedAt.field": "updatedAt",
    "transforms.convertUpdatedAt.target.type": "Timestamp",
    "transforms.convertUpdatedAt.format": "yyyy-MM-dd'T'HH:mm:ss.SSSSSSXXX",
    "consumer.override.auto.offset.reset": "latest",
    "quote.sql.identifiers": "never"
  }
}
